// Jonah Moore
//CSE 271 - B
// 9/4/2026
// Project 1, currently working through phase 1 of methods, overall project will use file reading skills.

import java.util.Scanner;

public class ScriptReviewTool {
    public static String getFileName(Scanner kb) {
        // Method used to get a file name from user input.
        System.out.print("Enter input file name: ");

        String fileName = kb.nextLine();

        return fileName;
    }

    public static int getUserChoice(Scanner kb) {
        // Method used to display a menu of choices the user can make,
        // takes in input only, actual execution of the choice will be done in main
        // method.

        System.out.print("Output options:\n1. Raw word list\n2. Palindrome list\n3. High value word list\n"
                + "4. Unique word list\nEnter choice: ");

        int userChoice = kb.nextInt();

        return userChoice;
    }

    public static String clean(String word) {
        // Method used to take in input string, and output a new string that has been
        // stripped of
        // all non-letters and white space, and is now in upper case.

        String oWord = "";

        for (int i = 0; i < word.length(); i++) {

            if (Character.isLetter(word.charAt(i))) {

                oWord += word.charAt(i);
            }

        }

        oWord = oWord.toUpperCase();

        return oWord;
    }

    public static boolean isPalindrome(String word) {
        // A method used to check if an input string is a palindrome (meaning that
        // it's a word that reads the same forwards and backwards). 
        
        word = clean(word);

        boolean boolReturn = true;
        if (word.length() == 1) {

            boolReturn = false;

            return boolReturn;
        }
        while (boolReturn = true) {
            for (int i = 0; i < word.length(); i++) {

                if (word.charAt(i) == word.charAt(word.length() - i)) {
                    boolReturn = true;
                }
                else {
                    boolReturn = false;
                }
            }
        }
        return boolReturn;
    }

    public static void main(String[] args) {

        //
        Scanner kb = new Scanner(System.in);

        String fileName = getFileName(kb);

        int userChoice = getUserChoice(kb);

        String tongueTwister = "A man, a plan, a canal. Panama";
        
        String palindrome = "rAcECar";
        
       // System.out.println("Is tongueTwister a palindrome? " + isPalindrome(tongueTwister));
        
        System.out.println("Is palindrome a palindrome? " + isPalindrome(palindrome));
    }
}
